//Import the DMS Class
var DMSClientChannel = require('./lib/dms-client-channel');

//Create and return a new instance of the class 
function createDMSInstance(config) {
    var dms_instance = new DMSClientChannel(config);
    return dms_instance;
}

//Export class instance
module.exports = createDMSInstance;